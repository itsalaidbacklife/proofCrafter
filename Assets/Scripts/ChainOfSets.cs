using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChainOfSets : MonoBehaviour
{
    public ArrayList chain;
    // Start is called before the first frame update
    void Start()
    {
        chain = new ArrayList();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
